export const icon = {
  w: 10,
  h: 10,
  mt: 4,
  mx: 'auto',
  color: 'green.400'
} as const

export const head = {
  mt: '-10px',
  textAlign: 'center',
  fontSize: { base: 'xl', md: '2xl' }
} as const

export const text = {
  fontSize: { base: 'sm', sm: 'md' }
} as const
