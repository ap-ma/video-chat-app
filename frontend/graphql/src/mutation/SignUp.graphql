input SignUpInput {
  code: String!
  name: String!
  email: String!
  password: String!
  passwordConfirm: String!
  comment: String
  avatar: Upload
}

mutation SignUp($input: SignUpInput!) {
  signUp(input: $input)
}
